info:
  name: search library
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/library/search"
  body:
    type: json
    data: '{"query": "hob"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
